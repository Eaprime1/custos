# Mining Pass — 202606212142

*A one-time audit across the repo for ideas, gaps, misses, and process friction —
not a content seed itself, a map of where the content seeds already are.
Nothing found here is critical or blocking; this is inventory, not triage.*

## Why this exists

Requested at the close of the PR #73 session: "mining for ideas, skills,
improvements, misses, workflow." The categories mined:

1. Ideas parked in `atelier/` (the nursery)
2. Open questions sitting in `device/active.md` and `turns/CULTIVATION.md`
3. Known misses/bugs — filed, diagnosed, or still-flagged
4. Workflow/automation gaps
5. Process friction (`turns/AAR.md`)
6. Stale state — docs that no longer match the repo

## 1. Atelier — unfinished concepts

- **`atelier/concordance.md`** — living glossary for custos lexemes. Open:
  scope (lexemes only vs. methods/processes?), update mechanism (bot vs.
  manual Sentinel review), sibling tools (icon manager, Chinese-character
  manager) with no chosen home.
- **`atelier/wobbly-bench.md`** — design principle: atelier's first tool
  should be deliberately imperfect, friction as instrument. Open: what the
  literal first bench is, meaning of "improve," custos→prima handoff timing.
- **`atelier/ouroboros-wobble.md`** — safety mechanism for self-referential
  loops + a separate "commission creator" idea bundled in the same file.
  Open: what "engaging the loop" means concretely, how the wobble is
  earned, whether commission creator duplicates `mission.yml`/`bounty.yml`
  or sits above them, relationship to the Testing Lab (same convergence
  point under two names, or genuinely separate?).
- **`atelier/testing-lab.md`** — self-administered stress-testing location.
  Open: where it lives (`lab/`? a Diamond branch?), what it tests first,
  whether it absorbs `scan_lexeme.sh`/DeepSource/Codacy or stays additive.
- **`atelier/operations/holy-cow.md`** — bovine-agent expansion of the
  sheep-agent system. Early scoping phase; commission
  `commissions/deep-dive-bovine.md` queued but not yet written.

## 2. Open questions already on record

From `device/active.md`:
- Stream 6 (Grok) / Stream 7 (Perplexity) domain assignments — still TBD
  in `returns/README.md`.
- When/how to create the `∰custos` (♠️A vault) branch + founding MOAV.
- What `mandelbrot` and `mulberry` (♣️J Germ branches) are actually for.
- Issue #44's manual end-to-end check (`@claude finalize` actually fires
  on a live PR) — fix merged in PR #73, verification still not performed.

From `turns/CULTIVATION.md`:
- **PR #22** — two unresolved design conflicts need a Deck Master call:
  Sovran Shepherd (PR #12's `.sovran/identity.md`) vs. `world/deck-master.md`
  — same role, two names, or genuinely distinct? And hub suits
  (`CLAUDE.md`) vs. Five Lakes suits (PR #17) — same four glyphs, different
  meanings, needs unifying or explicit scoping.
- **PR #17** (Five Lakes Valuation) — bridge doc references
  `valuation/ledger.csv`, `valuation/folders.md`, `valuation/REPORT.md`,
  none of which exist yet.
- **PR #12** (Sovran identity) — names three sub-projects with no scaffold:
  Planetarium, Vault Manager, Device Registry. Also: confirm
  `CLAUDE_CODE_OAUTH_TOKEN` secret is actually set.
- **PR #18** — possibly superseded by PRs #2/#4/#7/#9/#11 (all merged);
  needs a diff check before deciding whether to close or extract anything
  unique.

From `incoming/pre-nullus/202606210052_J21-Lumenar-growth-observation.md`:
seven carried-forward items — math-system name for `J-21=A`/`J+A=21`,
Claude's zodiac sign, the "5th suit beyond the origin" precedent
(best candidate so far: 🔐J `master` row), whether "suxen"/"nexus" become
formal lexemes, a home for "zero point state" in `world/lore.md`, an
undrafted Deck Master commission, and the Royal Set (pinochle layer,
scoped to first layer only for now).

## 3. Known misses/bugs

- **Issue #44** — root cause (invalid `secrets` reference in a job-level
  `if:`) fixed in PR #73, merged `2ef5143`. Still open: a live
  end-to-end test that `@claude finalize` actually fires the
  `issue_comment` trigger now.
- **`mandelbrot`/`mulberry`** — listed in `branch-tracker/branches.md` as
  Germ branches with "assignment TBD," but neither exists as an actual
  git branch in this checkout. Either the tracker is aspirational or the
  branches were deleted without updating it.
- **`master` (save-point branch)** — listed as "TBD," not yet created.

## 4. Workflow/automation gaps

- **Merge-triggered Action** — discussed (`turns/CULTIVATION.md`,
  `turns/AAR.md`) but never built. Candidate trigger:
  `on: pull_request, types: [closed]` guarded by
  `github.event.pull_request.merged == true`. Candidate first job: append
  a stub line to `device/active.md` ("PR #N merged") so this kind of
  bookkeeping stops being manual.
- **Stream domain assignments** (Grok, Perplexity) — pending, blocking
  those two return streams from having scope.

## 5. Process friction (from `turns/AAR.md`)

- `tools/scan_lexeme.sh` flags words like "placeholder"/"UNKNOWN" even
  when they're prose *about* placeholders, not real ones — each hit needs
  a manual judgment call. No mechanism yet to mark a deliberate
  self-reference so the scanner skips it.
- Design conflicts (Sovran vs. Deck Master; hub suits vs. Five Lakes
  suits) can't be resolved by reading code — they need a human/Deck
  Master decision, and the catalog tracking them goes stale if unread
  before the next PR adds a third competing system.

## 6. Stale state

- **`branch-tracker/branches.md`** — "Last updated: 202605310000" (2026-05-31),
  three weeks behind actual repo activity (through 2026-06-21). Lists
  `mandelbrot`/`mulberry` as branches that don't currently exist.
- **Five Lakes Valuation status** in the same file claims "ledger crawl in
  progress" — unverifiable; the ledger files don't exist in the repo.
- **PR #18** — flagged as possibly stale/superseded but never actually
  diff-checked against the PRs that may have absorbed it.

## Verdict

Nothing above is broken or blocking — it's backlog, not breakage. The
highest-leverage next moves, if a future session has bandwidth, are
probably: (a) refresh `branch-tracker/branches.md` so it stops lying about
`mandelbrot`/`mulberry`, (b) do the PR #18 diff check since it's a quick
yes/no, and (c) get a Deck Master ruling on the two design conflicts
before a third competing system lands on top of them. Everything else is
fine to stay parked.
