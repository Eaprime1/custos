# INCOMING

Staging ground for witness copies, ahead of vault.

Any repo producing a formal custody event drops its witness copy here
before Custos accepts it into `vault/`. Filing is not acceptance —
acceptance is a separate, deliberate step.

## Categories

- `pre-nullus/` — witness copies of pre-nullus events (see `pre-nullus/README.md`)
