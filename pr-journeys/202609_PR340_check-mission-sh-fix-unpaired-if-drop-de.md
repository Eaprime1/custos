# PR Journey: #340 — check_mission.sh: fix unpaired if; drop dead curl and duplicate guard

**Repository:** Eaprime1/custos  
**prima-clock:** 202609231041  
**Branch:** `claude/trusting-faraday-kdoq87-check-mission` → `main`  
**Author:** @Eaprime1  
**State:** FINALIZED (auto)  

## Intent

Make `tools/check_mission.sh` run again. On `main` it doesn't parse at all.

## What Arrived

These were flagged by the automated review on #339 and confirmed on `main`:
- **Syntax error:** line 42 repeated `if echo "$response" | jq -e '.message' …; then` with no matching `fi`, so `bash -n` failed with *unexpected end of file*. The duplicate `if` is gone.
- **Dead `curl`:** line 28's result was overwritten on the next line, and it used the unsafe `"${auth_header[@]}"` expansion, which fails under `set -u` when no token is set. It's gone. The remaining call uses the safe `${auth_header[@]+"${auth_header[@]}"}` form.
- **Unreachable guard:** lines 36–39 repeated the empty-response check. It's gone.

Net: −7 lines, no new behaviour. I checked it with `bash -n`, then ran it with a fake `curl` on the PATH to cover each path:

| Response | Output | Exit |
|---|---|---|
| empty | `error: empty response from GitHub API` | 1 |
| `{"message":"Bad credentials"}` | `error: GitHub API returned an error: Bad credentials` | 1 |
| merged PR mentioning Claude | `complete: …` + PR URL | 0 |
| no AI-faction PR | `incomplete: …` | 1 |

## Resonance

*parses again

---*

## The Arc

| Event | prima-clock | Actor |
|---|---|---|
| Opened | 202609230928 | @Eaprime1 |
| Auto-Finalized | 202609231041 | github-actions[bot] |

## CI Record

| Check | Result |
|---|---|
| Codacy Static Code Analysis | ✅ |
| DeepSource: Analysis | ⏭ |
| claude-review | ⏭ |
| validate | ✅ |
| dependency-review | ✅ |
| scan | ✅ |
| scan | ✅ |
| custos-speaks | ✅ |
| GitGuardian Security Checks | ✅ |

## DeepSource Record

**Passed:** 0  
**Failed:** 0  
**Skipped:** DeepSource: Analysis

## Review Scores

| Dimension | Score | Note |
|---|---|---|
| Correctness | 5/5 | 9 CI check(s) — all passed |
| Consistency | 5/5 | Description complete · ethics 5/5 |
| Scope | 5/5 | 1 file(s) changed |
| Verification | 5/5 | 9 check run(s) completed |
| **Valuation** | **High** | 20/20 |

## Ethics Check

- ✅ Entity agency respected (human, AI, concept — all contributors credited)
- ✅ Free to fork, remix, echo — no hidden ownership
- ✅ `bash tools/scan_lexeme.sh` run — no new hits
- ✅ No unintended harm surface in tools or scripts. Removes code only; the token is still sent only when `GITHUB_TOKEN` is set.
- ✅ Shell inputs validated where applicable. The mission argument check and the `jq` presence check are unchanged.

## What Door Does This Open?

Should a CI step run `bash -n` over `tools/*.sh`, so a script that doesn't parse can't reach `main` again?

---

**prime state:** 3  
**witnessed:** false

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01XVihLtjp6k5TStKRVFcQsD

---
_Generated by [Claude Code](https://claude.ai/code/session_01XVihLtjp6k5TStKRVFcQsD)_

---
**prima-clock:** 202609231041  
**witnessed:** true  
*🌿 Custos — the shepherd closes the fold · ∰🌿*