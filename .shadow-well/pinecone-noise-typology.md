# pinecone as the noise-typology layer

`prima-clock: 202609251844` · nowhere · from `pandora/germs/navigo2_germ.md`

The navigo2 preturn named six noise types in the inbox: temporal pressure, identity-triggered, gamified attention traps, ambient commercial radiation, platform migration debris, and genuine signal in noise costume.

Seed: embed each thread with a noise-type vector in Pinecone (active account confirmed in the germ). The pressure field becomes queryable by weight, frequency and identity-trigger type, not just sorted.

The semantic layer already exists. The content layer doesn't yet. Nothing is built from this until the Shepherd names it.
