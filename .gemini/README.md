# .gemini — nav3 workspace

**Navigo:** nav3 (Gemini + eaprime1)  
**Suit:** ♦️  
**Prima-clock (workspace created):** 202607022251

---

This folder is the working context for nav3 sessions. It holds:

- Source documents returned from Gemini sessions before they have a formal custody home
- Queued context for the next nav3 session (what to pick up, what was left open)
- nav3 stream-return materials pending filing to `returns/stream-1-language/`, `returns/stream-2-emotion/`, or `incoming/pre-nullus/`

## Conventions

- Files named `<prima-clock>_<slug>.md` follow the same convention as `incoming/pre-nullus/`
- Raw exports from Gemini sessions land here first, get renamed `.md` once reviewed, then file to `returns/`, `incoming/pre-nullus/`, or `atelier/`
- A `queue.md` (optional) can list what nav3 should tackle next session

## Current Contents

*(empty — nav3 fills this in on first session)*
